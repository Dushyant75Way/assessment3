import { Box, Toolbar } from "@mui/material";
import LoginForm from "../components/LoginForm";

const Login = () => {
  return (
    <Box>
      {/* <Toolbar /> */}
      <LoginForm />
    </Box>
  );
};

export default Login;
